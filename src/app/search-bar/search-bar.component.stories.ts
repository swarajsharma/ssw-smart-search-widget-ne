/*import { storiesOf } from '@storybook/angular';

import { CUSTOM_ELEMENTS_SCHEMA } from '@angular/core';
import { SearchBarComponent } from './search-bar.component';
import { SearchFormValues } from './search-form-values';

const searchBarFormData: SearchFormValues = {
  depAirport: [],
  goingTo: [],
  departureDate: {
    jsdate: new Date('2019-06-01'),
    date: {
      year: 2019,
      month: 6,
      day: 1,
    },
  },
  duration: '0',
  occupation: [{
    adults: 2,
    children: [],
  }],
  flexible: true,
};

storiesOf('HotelSearch', module)
  .add('Default', () => ({
    template: `<ow-search-bar [formValues]="formValues"></ow-search-bar>`,
    moduleMetadata: {
      declarations: [
        SearchBarComponent,
      ],
    },
    props: {
      formValues: searchBarFormData,
    },
  }));
*/
